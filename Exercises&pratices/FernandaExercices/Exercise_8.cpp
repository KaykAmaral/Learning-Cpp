/*
#include <iostream>

void testTriangle(double x, double y, double z) {

	if (x == y || y == z || x == z) {
		if (x == y && y == z) { // equilateral
			std::cout << "This triangle is equilateral";
		}
		if (x != y || x != z || y != z) { // isosceles
			std::cout << "This triangle is isosceles";
		}
	}
	if (x != y && x != z && y != z) { // scalene
		std::cout << "This triangle is scalene";
	}

}

int main() {
	double x, y, z;
	std::cout << "Text 3 triangle angles:\n";
	std::cout << "1st angle: ";
	std::cin >> x;
	std::cout << "2nd angle: ";
	std::cin >> y;
	std::cout << "3rd angle: ";
	std::cin >> z;

	testTriangle(x, y, z);

	return 0;
}
*/